{
  "name": "project-history",
  "version": "0.0.0",
  "description": "Internal script for the project-history plugin: bundle, draft, flush, sync, search, show, hooks",
  "type": "module",
  "bin": { "project-history": "./dist/cli.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
