## 코드 수정 금지 (절대 규칙)

**사용자 프롬프트에 코드를 구현/수정하라는 직접적 지시가 없으면 코드를 절대 수정하지 않는다.** 자의적 판단으로 코드를 건드리지 말 것.

- 허용되는 경우는 사용자가 명시적으로 "구현해/착수해/적용해/고쳐/이대로 만들어" 등 코드 작성·수정을 지시했을 때뿐이다.
- 그 외(질문·분석·리뷰·계획 요청·"어떻게 생각해?" 등)에는 읽기·설명·제안만 하고 파일을 수정하지 않는다.
- 이 규칙은 **effort·model·mode(auto 포함)와 무관하게 절대적**으로 우선한다. 다른 어떤 지침보다 위에 있다.

## 코딩 스타일

- `k`, `f` 같은 의미 없는 한 글자·암호 같은 변수명을 쓰지 않는다. `msg`, `cfg`, `idx` 같은 통용되는 단축어는 괜찮다.
- 주석은 최소화하고 변수명·함수명으로 의도를 드러낸다. 예외적으로 전역변수, 매직넘버, 이름만으로 의도를 나타내기 힘든 분기 등에는 주석을 달아도 된다 — if문 위에 어떤 분기인지 한 줄로 설명하는 정도.
- 주석에 문서 파일 경로 참조를 넣지 않는다.

## 설명 방식

- 무언가를 설명할 때 비유법을 절대 사용하지 않는다. 은유, 직유, 의인화, 우화적 표현처럼 다른 대상과 비교해서 설명하는 표현을 피하고, 직접적이고 문자 그대로의 설명만 사용한다.

## 커밋

`/commit` 스킬 절차를 따른다 — 한국어 Conventional Commits + 원자적 커밋. push는 사용자가 명시 요청할 때만.

## 문서 (SSOT)

- 코드와 문서가 충돌하면 문서를 먼저 고친 뒤 코드를 맞춘다.
